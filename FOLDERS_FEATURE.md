# Folders Feature - Flash Cards App

## 📁 Overview

تم إضافة نظام مجلدات شامل لتنظيم البطاقات التعليمية (Decks) بشكل هرمي.

## ✨ Features Added

### Backend

1. **Database Schema**

   - جدول `folders` جديد مع دعم المجلدات الفرعية
   - تحديث جدول `decks` لإضافة علاقة `folder_id`
   - دعم ترتيب المجلدات والبطاقات (`order` field)

2. **API Endpoints**

   - `GET /api/folders` - جلب جميع المجلدات
   - `POST /api/folders` - إنشاء مجلد جديد
   - `GET /api/folders/{id}` - جلب مجلد محدد
   - `PUT /api/folders/{id}` - تحديث مجلد
   - `DELETE /api/folders/{id}` - حذف مجلد
   - `POST /api/folders/{id}/move-deck` - نقل deck إلى مجلد
   - `POST /api/folders/remove-deck` - إزالة deck من مجلد

3. **Models**
   - `Folder` model مع علاقات للمجلدات الفرعية والبطاقات
   - تحديث `Deck` model لإضافة علاقة folder

### Frontend

1. **Components**

   - `FolderItem.js` - عرض المجلد مع المجلدات الفرعية والبطاقات
   - `FolderForm.js` - نموذج إنشاء/تعديل المجلدات
   - `FoldersView.js` - الواجهة الرئيسية للمجلدات

2. **Context**

   - `FoldersContext.js` - إدارة حالة المجلدات

3. **Features**
   - إنشاء مجلدات ومجلدات فرعية
   - عرض هرمي للمجلدات
   - سحب وإفلات البطاقات إلى المجلدات (Drag & Drop)
   - تعديل وحذف المجلدات
   - عرض إحصائيات المجلدات

## 🚀 How to Use

### إنشاء مجلد جديد

1. اضغط على زر "Folders" من الصفحة الرئيسية
2. اضغط "Create Folder"
3. أدخل اسم المجلد ووصفه
4. اضغط "Create Folder"

### إنشاء مجلد فرعي

1. اضغط على ⋮ بجانب المجلد
2. اختر "Add Subfolder"
3. أدخل البيانات واحفظ

### نقل Deck إلى مجلد

1. في صفحة المجلدات، اسحب البطاقة
2. أفلتها على المجلد المطلوب

### حذف مجلد

1. اضغط على ⋮ بجانب المجلد
2. اختر "Delete"
3. البطاقات والمجلدات الفرعية ستنتقل للمستوى الأعلى

## 🔧 Migration Required

لتفعيل الميزة، يجب تشغيل migration:

```bash
cd backend
php artisan migrate
```

## 📝 Database Structure

### folders table

- `id` - Primary Key
- `name` - اسم المجلد
- `description` - وصف اختياري
- `parent_folder_id` - معرف المجلد الأب (null للمجلدات الجذرية)
- `order` - ترتيب المجلد
- `timestamps`

### decks table (updated)

- Added `folder_id` - معرف المجلد الذي ينتمي إليه الـdeck
- Added `order` - ترتيب الـdeck داخل المجلد

## 🎨 UI/UX Features

- عرض عدد البطاقات في كل مجلد
- إحصائيات التقدم (عدد البطاقات المتعلمة)
- توسيع/طي المجلدات
- تأثيرات بصرية عند السحب والإفلات
- قوائم منسدلة للإجراءات السريعة
- تصميم متجاوب وسهل الاستخدام

## 🔒 Security

- منع الحلقات الدائرية (لا يمكن نقل مجلد إلى مجلد فرعي له)
- التحقق من صحة البيانات على مستوى الـBackend
- معالجة الأخطاء بشكل صحيح

## 🐛 Known Limitations

- لا يوجد دعم لإعادة ترتيب المجلدات بالسحب والإفلات حالياً
- يجب تحديث الصفحة يدوياً بعد بعض العمليات

## 🔜 Future Enhancements

- [ ] إعادة ترتيب المجلدات بالسحب والإفلات
- [ ] نسخ/نقل متعدد للبطاقات
- [ ] بحث في المجلدات
- [ ] أيقونات مخصصة للمجلدات
- [ ] مشاركة المجلدات
