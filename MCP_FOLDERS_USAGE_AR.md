# 📁 دليل استخدام المجلدات عبر MCP

## 🎯 نظرة عامة

**MCP Flashcards Server** يدعم الآن إدارة المجلدات (Folders) بشكل كامل! يمكنك تنظيم مجموعات البطاقات (Decks) في مجلدات ومجلدات فرعية دون حدود.

---

## 🚀 البدء السريع

### 1️⃣ **تفعيل دعم المجلدات**

قم بتشغيل السكريبت التالي في PowerShell:

```powershell
.\activate-mcp-folders.ps1
```

أو قم بالخطوات يدوياً:

```bash
cd tools/mcp/flashcards-server
npm install
npm run build
```

### 2️⃣ **أعد تشغيل VS Code**

بعد البناء، أعد تشغيل VS Code لتحميل الـ MCP Server المحدث.

### 3️⃣ **ابدأ باستخدام الأوامر**

الآن يمكنك استخدام GitHub Copilot للتحكم بالمجلدات!

---

## 📝 الأوامر المتاحة

### 🔍 **1. عرض جميع المجلدات**

```
💬 عرض جميع المجلدات
💬 اعرض لي المجلدات الموجودة
💬 ما هي المجلدات المتاحة؟
```

**ما يحدث:**

- يعرض جميع المجلدات في شكل هرمي
- يعرض المجلدات الفرعية
- يعرض البطاقات (Decks) داخل كل مجلد

---

### ➕ **2. إنشاء مجلد جديد**

#### مجلد عادي (في المستوى الرئيسي):

```
💬 أنشئ مجلد باسم "البرمجة"
💬 اصنع مجلد جديد اسمه "علوم الحاسب"
💬 Create a folder named "Computer Science"
```

#### مجلد فرعي (داخل مجلد آخر):

```
💬 أنشئ مجلد "JavaScript" داخل مجلد "البرمجة"
💬 اصنع مجلد فرعي "React" في مجلد "JavaScript"
💬 Create a subfolder "Python" under "Programming"
```

**ملاحظة:** يمكنك إنشاء مستويات متعددة من المجلدات دون حدود!

---

### ✏️ **3. تحديث مجلد**

```
💬 غير اسم المجلد 3 إلى "هندسة البرمجيات"
💬 حدّث وصف المجلد "البرمجة" إلى "مجلد لكل مواضيع البرمجة"
💬 Update folder 5 name to "Web Development"
```

---

### 🗑️ **4. حذف مجلد**

```
💬 احذف المجلد 7
💬 امسح مجلد "Test"
💬 Delete folder "Old Stuff"
```

**ملاحظة:**

- عند حذف مجلد، تنتقل البطاقات والمجلدات الفرعية إلى المستوى الأعلى
- لا يتم حذف البطاقات نفسها

---

### 🔄 **5. نقل مجموعة إلى مجلد**

```
💬 انقل المجموعة 5 إلى مجلد "البرمجة"
💬 ضع مجموعة "JavaScript Basics" في مجلد "Web Dev"
💬 Move deck 12 to folder "Computer Science"
```

---

### ↩️ **6. إخراج مجموعة من مجلد**

```
💬 أخرج المجموعة 8 من مجلدها
💬 احذف مجموعة "Test Deck" من المجلد
💬 Remove deck 15 from its folder
```

**ملاحظة:** المجموعة ستنتقل إلى المستوى الرئيسي (root level).

---

## 🎨 أمثلة متقدمة

### 📚 **تنظيم البطاقات حسب الموضوع**

```
💬 نظم بطاقاتي في مجلدات حسب الموضوع:
   - أنشئ مجلد "برمجة"
   - أنشئ مجلد "لغات"
   - أنشئ مجلد "رياضيات"
   - انقل جميع بطاقات JavaScript إلى مجلد "برمجة"
```

### 🌳 **بناء هيكل متداخل**

```
💬 أريد هيكل مجلدات كالتالي:
   - علوم الحاسب
     - البرمجة
       - JavaScript
       - Python
     - قواعد البيانات
       - SQL
       - NoSQL
```

### 🔍 **البحث والتنظيم**

```
💬 اعرض جميع المجلدات وأخبرني أي مجلد يحتوي على أكثر بطاقات
💬 أنشئ مجلد "للمراجعة" وانقل إليه جميع البطاقات الصعبة
```

---

## 🛠️ الأدوات التقنية (للمطورين)

إذا كنت تستخدم MCP مباشرة، هذه هي الأدوات المتاحة:

### `listFolders`

```json
{
  "name": "listFolders",
  "description": "List all folders with their decks and subfolders"
}
```

### `createFolder`

```json
{
  "name": "createFolder",
  "required": ["name"],
  "properties": {
    "name": "string",
    "description": "string (optional)",
    "parent_folder_id": "number (optional)"
  }
}
```

### `updateFolder`

```json
{
  "name": "updateFolder",
  "required": ["folderId"],
  "properties": {
    "folderId": "number",
    "name": "string (optional)",
    "description": "string (optional)"
  }
}
```

### `deleteFolder`

```json
{
  "name": "deleteFolder",
  "required": ["folderId"],
  "properties": {
    "folderId": "number"
  }
}
```

### `moveDeckToFolder`

```json
{
  "name": "moveDeckToFolder",
  "required": ["folderId", "deckId"],
  "properties": {
    "folderId": "number",
    "deckId": "number"
  }
}
```

### `removeDeckFromFolder`

```json
{
  "name": "removeDeckFromFolder",
  "required": ["deckId"],
  "properties": {
    "deckId": "number"
  }
}
```

---

## ❓ الأسئلة الشائعة

### ❔ **هل يمكنني إنشاء مجلدات متداخلة بدون حدود؟**

✅ نعم! يمكنك إنشاء مجلدات داخل مجلدات بدون حد للعمق.

### ❔ **ماذا يحدث للبطاقات عند حذف مجلد؟**

✅ البطاقات والمجلدات الفرعية تنتقل إلى المستوى الأعلى (parent level). لا يتم حذف البطاقات.

### ❔ **هل يمكنني نقل مجلد داخل مجلد آخر؟**

✅ نعم، يمكنك تحديث `parent_folder_id` لنقل مجلد.

### ❔ **كيف أتحقق من أن MCP يعمل؟**

```
💬 عرض جميع المجلدات
```

إذا عرضت لك قائمة (حتى لو كانت فارغة)، فالنظام يعمل!

### ❔ **لماذا لا تظهر الأدوات؟**

- تأكد من بناء المشروع: `npm run build` في `tools/mcp/flashcards-server`
- أعد تشغيل VS Code
- تحقق من `.vscode/mcp.json` أن `flashcards` مسجل

---

## 📊 البنية التقنية

```
Backend (Laravel)
├── Database
│   ├── folders table
│   │   ├── id
│   │   ├── name
│   │   ├── description
│   │   ├── parent_folder_id
│   │   └── order
│   └── decks table
│       └── folder_id (foreign key)
│
├── API Endpoints
│   ├── GET    /api/folders
│   ├── POST   /api/folders
│   ├── PUT    /api/folders/{id}
│   ├── DELETE /api/folders/{id}
│   ├── POST   /api/folders/{id}/move-deck
│   └── POST   /api/folders/remove-deck
│
└── FolderController
    ├── index()
    ├── store()
    ├── show()
    ├── update()
    ├── destroy()
    ├── moveDeck()
    └── removeDeck()

MCP Server (TypeScript)
├── src/
│   ├── index.ts (6 folder tools)
│   ├── schemas.ts (FolderSchema)
│   └── http.ts (API client)
└── dist/ (compiled JavaScript)
```

---

## 🎉 الخلاصة

الآن أصبح لديك نظام مجلدات كامل ومتقدم! يمكنك:

✅ تنظيم مجموعات البطاقات في مجلدات  
✅ إنشاء هياكل متداخلة معقدة  
✅ نقل البطاقات بسهولة  
✅ التحكم الكامل عبر AI (Copilot)

---

## 📚 مراجع إضافية

- 📄 `FOLDERS_FEATURE.md` - الوثائق التقنية الكاملة
- 📄 `FOLDERS_IMPLEMENTATION_SUMMARY.md` - ملخص التنفيذ
- 📄 `FOLDERS_USER_GUIDE_AR.md` - دليل المستخدم التفصيلي
- 📄 `MCP_FOLDERS_UPDATE.md` - تحديثات MCP

---

**🚀 استمتع بتنظيم بطاقاتك بكفاءة!**
