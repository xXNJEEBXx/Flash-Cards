[phases.setup]
# Include PHP 8.2, Composer, and DB extensions (SQLite + MySQL)
nixPkgs = [
    "php82",
    "php82Packages.composer",
    "php82Extensions.sqlite3",
    "php82Extensions.pdo_sqlite",
    "php82Extensions.pdo_mysql"
]
aptPkgs = []

[phases.install]
cmds = ["composer install --optimize-autoloader --no-dev"]

[phases.build]
cmds = [
    "chmod +x init-db.sh",
    "chmod +x start.sh"
]

[start]
cmd = "bash start.sh"

# No static env here; Railway Variables provide DB_* for MySQL
