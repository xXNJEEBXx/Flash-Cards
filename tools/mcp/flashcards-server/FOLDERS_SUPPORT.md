# 📁 MCP Tool - دعم المجلدات (Folders Support)

## ✨ الميزات الجديدة المضافة:

تم تحديث MCP Server ليدعم إدارة المجلدات كاملة! الآن يمكن للـ AI التحكم بالمجلدات والبطاقات معاً.

---

## 🛠 Tools الجديدة للمجلدات:

### 1. **listFolders** - عرض جميع المجلدات

```json
{
  "name": "listFolders",
  "description": "List all folders with their decks and subfolders"
}
```

**مثال على الاستخدام:**

```
AI: عرض جميع المجلدات
→ يعرض كل المجلدات مع البطاقات والمجلدات الفرعية
```

---

### 2. **createFolder** - إنشاء مجلد جديد

```json
{
  "name": "createFolder",
  "required": ["name"],
  "properties": {
    "name": "اسم المجلد",
    "description": "وصف المجلد (اختياري)",
    "parent_folder_id": "ID المجلد الأب (للمجلدات المتداخلة)"
  }
}
```

**أمثلة:**

```
AI: أنشئ مجلد باسم "البرمجة"
→ ينشئ مجلد جديد في المستوى الرئيسي

AI: أنشئ مجلد فرعي "JavaScript" داخل مجلد "البرمجة"
→ ينشئ مجلد متداخل
```

---

### 3. **updateFolder** - تحديث مجلد

```json
{
  "name": "updateFolder",
  "required": ["folderId"],
  "properties": {
    "folderId": "رقم المجلد",
    "name": "الاسم الجديد",
    "description": "الوصف الجديد"
  }
}
```

**مثال:**

```
AI: غير اسم المجلد 5 إلى "علوم الحاسب"
→ يحدث اسم المجلد
```

---

### 4. **deleteFolder** - حذف مجلد

```json
{
  "name": "deleteFolder",
  "required": ["folderId"],
  "properties": {
    "folderId": "رقم المجلد"
  }
}
```

**ملاحظة:** البطاقات والمجلدات الفرعية تنتقل للمستوى الأعلى

**مثال:**

```
AI: احذف المجلد رقم 3
→ يحذف المجلد وينقل محتوياته
```

---

### 5. **moveDeckToFolder** - نقل بطاقة لمجلد

```json
{
  "name": "moveDeckToFolder",
  "required": ["folderId", "deckId"],
  "properties": {
    "folderId": "ID المجلد المستهدف",
    "deckId": "ID البطاقة المراد نقلها"
  }
}
```

**مثال:**

```
AI: انقل البطاقة رقم 10 إلى مجلد "الأعمال الإلكترونية"
→ تنتقل البطاقة للمجلد
```

---

### 6. **removeDeckFromFolder** - إخراج بطاقة من مجلد

```json
{
  "name": "removeDeckFromFolder",
  "required": ["deckId"],
  "properties": {
    "deckId": "ID البطاقة"
  }
}
```

**مثال:**

```
AI: أخرج البطاقة رقم 10 من المجلد
→ البطاقة تنتقل للقائمة الرئيسية
```

---

## 📋 سيناريوهات عملية:

### السيناريو 1: تنظيم البطاقات في مجلدات

```
User: نظم بطاقاتي في مجلدات حسب المواضيع

AI Actions:
1. listDecks() - عرض جميع البطاقات
2. createFolder({ name: "Cybersecurity" })
3. createFolder({ name: "Programming" })
4. moveDeckToFolder({ folderId: 1, deckId: 5 })
5. moveDeckToFolder({ folderId: 2, deckId: 7 })

Result: ✅ تم تنظيم البطاقات في مجلدات
```

---

### السيناريو 2: إنشاء هيكل متداخل

```
User: أنشئ مجلد "علوم الحاسب" ومجلد فرعي "البرمجة" داخله

AI Actions:
1. createFolder({ name: "علوم الحاسب" })
   → يرجع: { id: 5, name: "علوم الحاسب" }
2. createFolder({
     name: "البرمجة",
     parent_folder_id: 5
   })

Result: ✅ هيكل متداخل جاهز
```

---

### السيناريو 3: نقل بطاقة بين المجلدات

```
User: انقل بطاقة "JavaScript Basics" من مجلد "General" إلى "Programming"

AI Actions:
1. listFolders() - البحث عن IDs
2. listDecks() - البحث عن البطاقة
3. removeDeckFromFolder({ deckId: 10 })
4. moveDeckToFolder({ folderId: 3, deckId: 10 })

Result: ✅ تم النقل بنجاح
```

---

## 🔧 الملفات المحدثة:

### 1. **schemas.ts** - إضافة FolderSchema

```typescript
export const FolderSchema = z.object({
  id: z.number(),
  name: z.string(),
  description: z.string().nullable().optional(),
  parent_folder_id: z.number().nullable().optional(),
  order: z.number().optional(),
  decks: z.array(DeckSchema).optional(),
  subfolders: z.lazy(() => z.array(FolderSchema)).optional(),
});
```

### 2. **index.ts** - إضافة 6 tools جديدة

- listFolders
- createFolder
- updateFolder
- deleteFolder
- moveDeckToFolder
- removeDeckFromFolder

---

## 🚀 كيفية البناء والتشغيل:

### 1. بناء المشروع:

```bash
cd tools/mcp/flashcards-server
npm run build
```

### 2. إعادة تشغيل MCP Server:

بعد البناء، أعد تشغيل VS Code أو Claude Desktop لتفعيل التحديثات

### 3. اختبار الميزات:

```bash
npm test
```

---

## 📊 مثال كامل:

### الأمر:

```
أنشئ مجلد "الأعمال الإلكترونية" وانقل إليه جميع البطاقات المتعلقة بالتجارة الإلكترونية
```

### خطوات AI:

```javascript
// 1. إنشاء المجلد
createFolder({
  name: "الأعمال الإلكترونية",
  description: "بطاقات التجارة الإلكترونية والأعمال الرقمية",
});
// → { id: 5, name: "الأعمال الإلكترونية" }

// 2. عرض البطاقات
listDecks();
// → يعرض جميع البطاقات

// 3. نقل البطاقات المناسبة
moveDeckToFolder({ folderId: 5, deckId: 10 });
moveDeckToFolder({ folderId: 5, deckId: 11 });
moveDeckToFolder({ folderId: 5, deckId: 12 });

// Result: ✅ تم التنظيم بنجاح!
```

---

## 🎯 الفوائد:

✅ **تنظيم أفضل** - AI يمكنه تنظيم البطاقات تلقائياً  
✅ **إدارة كاملة** - إنشاء، تحديث، حذف، نقل  
✅ **مجلدات متداخلة** - دعم المجلدات الفرعية  
✅ **ذكاء أكبر** - AI يفهم السياق ويقترح التنظيم  
✅ **أوامر طبيعية** - تحدث مع AI بشكل طبيعي

---

## 📝 ملاحظات مهمة:

### عند حذف مجلد:

- ✅ المجلد يُحذف
- ✅ البطاقات داخله تنتقل للمستوى الأعلى
- ✅ المجلدات الفرعية تنتقل للمستوى الأعلى

### عند نقل بطاقة:

- ✅ تنتقل فوراً
- ✅ تظهر في المجلد الجديد
- ✅ تختفي من المكان القديم

---

## 🧪 اختبار سريع:

بعد البناء، جرب هذه الأوامر مع AI:

```
1. "عرض جميع المجلدات"
2. "أنشئ مجلد باسم Test"
3. "انقل البطاقة الأولى إلى مجلد Test"
4. "أخرج البطاقة من مجلد Test"
5. "احذف مجلد Test"
```

---

**تاريخ التحديث:** 18 أكتوبر 2025  
**الإصدار:** 0.2.0 (مع دعم المجلدات)
