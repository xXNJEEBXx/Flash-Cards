{
  "name": "flashcards-mcp-server",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "MCP server to control Flash Cards via Laravel REST API (with Folders support)",
  "main": "dist/index.js",
  "bin": {
    "flashcards-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p .",
    "dev": "ts-node-esm src/index.ts",
    "start": "node dist/index.js",
    "check": "tsc -p . --noEmit",
    "test": "node ./tests/smoke.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "zod": "^3.23.8",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "ts-node": "^10.9.2",
    "@types/node": "^20.11.30"
  }
}
