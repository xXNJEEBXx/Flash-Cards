{
  "buildCommand": "npm --prefix ./flash-cards run build",
  "outputDirectory": "flash-cards/build",
  "installCommand": "npm --prefix ./flash-cards install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
