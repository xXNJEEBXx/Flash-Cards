# 🔧 حل مشكلة "Failed to create folder"

## 🎯 السبب
المشكلة تحدث لأن جدول `folders` غير موجود في قاعدة البيانات. يجب تشغيل migration أولاً.

---

## ✅ الحل السريع (طريقتان)

### الطريقة الأولى: استخدام السكريبت الجاهز ⚡
```powershell
# من مجلد المشروع الرئيسي
.\QUICK_FIX_MIGRATION.ps1
```

هذا السكريبت سيقوم بـ:
- ✅ التحقق من وجود Backend
- ✅ التحقق من قاعدة البيانات
- ✅ تشغيل Migration تلقائياً
- ✅ التحقق من نجاح العملية

### الطريقة الثانية: يدوياً 🔨
```powershell
# 1. انتقل لمجلد Backend
cd backend

# 2. شغّل Migration
php artisan migrate

# 3. ارجع للمجلد الرئيسي
cd ..
```

---

## 🔄 بعد تشغيل Migration

1. **حدّث المتصفح** (اضغط F5)
2. **اضغط على زر "Folders"**
3. **جرب إنشاء مجلد جديد**

---

## ✨ يجب أن تعمل الآن!

إذا نجحت العملية، ستتمكن من:
- ✅ إنشاء مجلدات جديدة
- ✅ إنشاء مجلدات فرعية
- ✅ نقل البطاقات للمجلدات
- ✅ تعديل وحذف المجلدات

---

## 🐛 إذا استمرت المشكلة

### تحقق من الأساسيات:

#### 1️⃣ هل Backend يعمل؟
```powershell
# في terminal جديد
cd backend
php artisan serve
```
يجب أن يعمل على: http://localhost:8000

#### 2️⃣ هل Frontend يعمل؟
```powershell
# في terminal آخر
npm run start:frontend
```
يجب أن يعمل على: http://localhost:3000

#### 3️⃣ تحقق من قاعدة البيانات
```powershell
cd backend
php artisan db:show
```
يجب أن ترى جدول `folders`

#### 4️⃣ افتح Console في المتصفح
- اضغط F12
- اذهب لتبويب "Console"
- ابحث عن أي أخطاء حمراء

---

## 📋 الأخطاء الشائعة وحلولها

### ❌ "php: command not found"
**الحل**: ثبّت PHP من https://www.php.net/downloads

### ❌ "composer: command not found"
**الحل**: ثبّت Composer من https://getcomposer.org

### ❌ "Class 'SQLite3' not found"
**الحل**: 
1. افتح `php.ini`
2. فعّل: `extension=sqlite3`
3. أعد تشغيل PHP

### ❌ "Migration table not found"
**الحل**:
```powershell
cd backend
php artisan migrate:install
php artisan migrate
```

### ❌ "CORS error" في Console
**الحل**: تأكد من تشغيل Backend على المنفذ 8000

---

## 🆘 المساعدة الإضافية

إذا لم تحل المشكلة:

1. **تحقق من الـlogs**:
   ```powershell
   cd backend/storage/logs
   # اقرأ آخر ملف laravel.log
   ```

2. **تحقق من .env**:
   ```
   DB_CONNECTION=sqlite
   DB_DATABASE=database/database.sqlite
   ```

3. **امسح الـcache**:
   ```powershell
   cd backend
   php artisan cache:clear
   php artisan config:clear
   ```

4. **أعد بناء database**:
   ```powershell
   cd backend
   rm database/database.sqlite
   touch database/database.sqlite  # أو new-item في PowerShell
   php artisan migrate --force
   ```

---

## 📞 تواصل

إذا استمرت المشكلة بعد كل المحاولات، أرسل:
- لقطة شاشة من الخطأ
- محتوى Console في المتصفح (F12)
- نتيجة `php artisan migrate`

---

**🎉 بالتوفيق!**
